{
  "name": "livekit-token-generator",
  "version": "1.0.0",
  "description": "LiveKit token generator - Serverless Cloud Function",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run build && npx @google-cloud/functions-framework --target=generateToken --source=dist",
    "build": "tsc",
    "predeploy": "npm run build",
    "deploy:gcp": "gcloud functions deploy generateToken --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point generateToken --source ."
  },
  "keywords": [
    "livekit",
    "token",
    "serverless",
    "cloud-functions"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "livekit-server-sdk": "^2.15.0"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "typescript": "^5.9.3"
  }
}
